/* 
        String str = "12345";

        // Check if the string contains only digits
        boolean isNumeric = str.matches("\\d+");

        System.out.println(isNumeric);  // Output: true
    
    
       // Check if the string starts with "abc" and ends with three digits
       boolean matchesPattern = str.matches("abc\\d{3}");
       
       
       
        // Check if the string matches "hello" in a case-sensitive manner
        boolean caseSensitiveMatch = str.matches("hello");
        
        Common Regular Expressions Used with matches()
        
	\\d – Any digit (0-9).
	\\D – Any non-digit.
	[a-zA-Z] – Any alphabetic character (lowercase or uppercase).
	. – Any character (except newline).
	^ and $ – Start and end of the string.
	\\s – Whitespace character (spaces, tabs, etc.).
	\\w – Any word character (letters, digits, or underscores).
}

 * 
 */
